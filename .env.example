# Kuadra OS — Supabase (proyecto kuadra-os, org Kuadra Film)
# Copia este archivo a .env.local. Estas dos son públicas por diseño:
# viajan al navegador y quien protege los datos es RLS, no su secreto.
NEXT_PUBLIC_SUPABASE_URL=https://maoqrpcxlirbzpxzwdid.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_E4jXSrk2Zxg8USPsyh5UHA_zy6mkhG6

# Sólo para scripts/create-user.mjs. Se saca del dashboard cuando se ocupe,
# va únicamente en .env.local y NUNCA se commitea: salta todo el RLS.
# SUPABASE_SERVICE_ROLE_KEY=

# Nota de despliegue: `vercel --prod` desde esta carpeta falla con
# "Deployment Blocked" mientras el repo tenga un remote de git y la cuenta de
# Vercel no tenga conectada su cuenta de GitHub (Settings → Login Connections).
# Mientras tanto: `git remote remove origin`, desplegar, y volver a añadirlo.
